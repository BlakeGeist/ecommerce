<%= render 'templates/header' %>

<main>

  <div class="categories">

    <% @categories.each do |category| %>

      <%= render '/templates/category_products', product: category_product(category), category: category  %>

    <% end %>

  </div>

</main>

<%= render 'templates/footer' %>
